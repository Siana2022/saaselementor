# fixtures/

JSON reales exportados de Elementor (kits y widgets). **Fuente de verdad** para
la ingeniería inversa: nunca se inventa la estructura del JSON de Elementor.

Se analizan con `scripts/analyze-fixture.ts` (PASO 2) para derivar los schemas
Zod del esquema propietario de Elementor.

> Pendiente: el usuario proporcionará los `.json` de referencia por widget/kit.
